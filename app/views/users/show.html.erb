
<div class="profile-container">
  <%# <div class="row"> %>
    <div class=col-4>
      <div class="profile-avatar-card">
         <% if current_user.avatar.attached?  %>
          <%= image_tag current_user.avatar, class: "rounded-circle" %>
         <% else %>
           <img alt="image" width="100" height="100"class="rounded-circle" src="https://en.gravatar.com/userimage/221761198/0609cffc0486a8272ef5f38096082c5d.png?size=200">
         <% end %>

        <%# <%= @user.name %>

      </div>
    <%= link_to "Edit Profile", edit_user_registration_path(current_user), class: "btn btn-ghost" %>
    <%= link_to "Edit Profile", edit_user_registration_path(current_user), class: "btn btn-ghost" %>
    <%= link_to "Edit Profile", edit_user_registration_path(current_user), class: "btn btn-ghost" %>

    </div>
  <%# </div> %>
</div>
